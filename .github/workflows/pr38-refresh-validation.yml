name: PR38 Refreshed Validation Stack

on:
  push:
    branches:
      - feature/06-dynamic-species-validation-prep

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install
        run: npm ci --no-audit --no-fund
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Targeted validation stack
        run: npm test -- tests/dynamic-species-registry.validation.test.ts tests/dynamic-species-registry.test.ts tests/generated-species-reference-enrichment.test.ts tests/game/generatedSpeciesKnowledge.test.ts tests/reaction-progression.test.ts
      - name: Full test suite
        run: npm test
      - name: Build
        run: npm run build
      - name: Record tested commit
        run: echo "TESTED_SHA=$GITHUB_SHA"
