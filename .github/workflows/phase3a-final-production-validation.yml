name: Phase3A Final Production Validation

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install
        run: npm ci --no-audit --no-fund
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Targeted final Phase3A regression
        run: npm test -- tests/reaction-progression.test.ts tests/phase3a-reaction-network.test.ts tests/phase3a-kinetics-thermal.test.ts tests/phase3a-dimensioned-resolver.test.ts tests/reaction-evaluation.test.ts tests/thermal.test.ts tests/dynamic-species-registry.test.ts tests/generated-species-reference-enrichment.test.ts tests/game/generatedSpeciesKnowledge.test.ts tests/ui/LaboratoryWorkspace.test.tsx
      - name: Determinism repeat
        run: |
          npm test -- tests/reaction-progression.test.ts tests/phase3a-reaction-network.test.ts tests/phase3a-kinetics-thermal.test.ts tests/ui/LaboratoryWorkspace.test.tsx
          npm test -- tests/reaction-progression.test.ts tests/phase3a-reaction-network.test.ts tests/phase3a-kinetics-thermal.test.ts tests/ui/LaboratoryWorkspace.test.tsx
      - name: Full test suite
        run: npm test
      - name: Build
        run: npm run build
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Browser smoke
        run: |
          npm run preview -- --host 127.0.0.1 --port 4173 &
          preview_pid=$!
          trap 'kill $preview_pid || true' EXIT
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173 >/dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:4173 >/dev/null
          npm run smoke:ui
      - name: Record tested commit
        run: echo "TESTED_SHA=$GITHUB_SHA"
