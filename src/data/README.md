# Chemistry Data Runtime

Normalized runtime data used by the browser simulation. Raw research/provenance assets and offline preparation tooling may live separately under `tools/` or future data directories.
